{
  "name": "my-new-2048",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "vite.config.js",
  "scripts": {
    "build": "tsc && vite build && rsync -av --delete oldGames/ dist/public/assets/ && firebase deploy && git add . && git commit -m 'first Commit' && git push -u origin main",
    "dev": "vite",
    "preview": "vite preview"
  },
  "dependencies": {
    "firebase": "^12.7.0",
    "typescript": "^5.9.3",
    "vite": "^7.3.0"
  }
}
